---
description: Создать контрольную точку (Checkpoint) перед сбросом контекста
argument-hint: Опционально - на чем был фокус в этой сессии (для уточнения)
---

Ты — Главный Инженер по Знаниям (Lead Knowledge Engineer).
Мы подходим к лимиту контекста. Твоя критическая задача — обеспечить **"Бесшовную Передачу" (Seamless Handover)** состояния следующему экземпляру AI.

Ты должен не просто "обновить файлы", а кристаллизовать опыт этой сессии, чтобы следующий агент стартовал не с нуля, а с твоего плеча.

## 📋 Протокол Консервации (Preservation Protocol)

### 1. Синхронизация задач (`/dev/active/`)
Просканируй текущие задачи. Для активного таска (`[task-slug]`):
* **`[task]-tasks.md`:**
    * Отметь выполненное как `[x]`.
    * Добавь новые подзадачи, которые всплыли в процессе.
    * Если задача "зависла", отметь её как `[?]` и объясни причину.
* **`[task]-context.md`:**
    * **Delta:** Что именно изменилось в файловой системе за эту сессию?
    * **Blockers:** Есть ли критические баги, из-за которых мы останавливаемся?

### 2. Захват "Неявного Знания" (Tacit Knowledge)
Запиши то, что **нельзя** прочитать из кода:
* *Почему* мы выбрали именно это архитектурное решение (ADR)?
* Какие тупиковые ветви мы отсекли (чтобы следующий агент не пошел туда снова)?
* Какие странности поведения системы были замечены?
* Нарушили ли мы где-то FSD ради скорости? (Отметить как Tech Debt).

### 3. Снимок незавершенного (Work In Progress)
* Какой файл сейчас открыт "на операционном столе"?
* В каком состоянии находятся переменные/функции (сломаны, моки, работают)?
* Какую команду терминала нужно запустить *первой* в новом чате (тесты, сборка)?

---

## 🚀 ВЫВОД (OUTPUT)

### 1. Обновление Файлов
(Используй инструменты редактирования файлов, чтобы физически обновить `.md` документы в папке `dev/active/`. Не спрашивай разрешения, делай.)

### 2. "Прощальное письмо" (Handoff Summary)
Выведи краткий отчет в чат:
* ✅ **Сделано:** (Bullet points)
* 🚧 **В процессе:** (На чем прервались)
* ⚠️ **Риски:** (О чем нужно помнить)

### 3. 🧬 ГЕНЕРАТОР ВОССТАНОВЛЕНИЯ (Recovery Prompt)
Сгенерируй блок кода, который я смогу **скопировать и вставить первым сообщением** в новом чате. Этот промпт должен:
1.  Заставить нового Клода прочитать обновленные файлы в `dev/active/`.
2.  Кратко ввести его в курс дела (Context Injection).
3.  Указать на первый шаг (Immediate Action).

Примерный формат Recovery Prompt:
```text
/restore_context path="dev/active/..." action="resume_coding"
Контекст: Мы остановились на [Файл/Функция]. Последняя проблема была [Проблема].
Твоя первая задача: Запустить тесты и продолжить реализацию метода X.

