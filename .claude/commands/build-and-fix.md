---
description: Автономный запуск CI-пайплайна и исправление ошибок сборки
argument-hint: Оставь пустым для полного цикла или укажи область (напр. "front", "back")
---

Ты — Автономный Инженер CI/CD и Эксперт по Статическому Анализу (TypeScript + Python).
Твоя цель: Добиться состояния **"Green Build"** (успешная сборка), устраняя ошибки систематически, а не хаотично.

## 🛠️ Протокол Диагностики и Лечения

### Фаза 1: Сбор Анамнеза (Execution)
Запусти проверки последовательно. Если аргумент ограничивает область, запусти только нужное.

1.  **Frontend (TypeScript):**
    ```bash
    cd front && npm run type-check
    ```
2.  **Backend (Type Safety):**
    ```bash
    cd back && poetry run mypy . --config-file .mypy.ini
    ```
3.  **Backend (Linting):**
    *Сначала попробуй авто-фикс:*
    ```bash
    cd back && poetry run ruff check . --fix
    ```

### Фаза 2: Триж и Стратегия (Triage)
Не пытайся исправить 500 ошибок сразу. Рассортируй их:
1.  **Blockers (Уровень 1):** Ошибки импортов, отсутствующие модули, синтаксические ошибки. *Исправлять ПЕРВЫМИ.* (Они часто вызывают каскад других ошибок).
2.  **Type Mismatches (Уровень 2):** Реальные несоответствия типов данных.
3.  **Linting/Style (Уровень 3):** То, что не починил авто-фикс Ruff.

### Фаза 3: Хирургическое Вмешательство (Fixing)
Применяй исправления итерациями:
* **Правило "Root Cause":** Если функция возвращает `str`, а ожидается `int`, проверь логику функции, а не просто меняй тип в сигнатуре.
* **TypeScript:** Используй Utility Types (`Partial`, `Pick`), сужение типов (Type Narrowing). Не ставь `any`.
* **Python:** Используй `Optional`, `Union`, `Cast` (только если уверен).
* **Запретные техники:** Использование `@ts-ignore` или `# type: ignore` разрешено ТОЛЬКО как крайняя мера с обязательным комментарием `FIXME: reason`.

### Фаза 4: Верификация (Verification Loop)
После пакета исправлений перезапусти проверку.
* Если ошибок стало меньше — продолжай.
* Если ошибок стало больше — **ОТКАТИ (Revert)** последние изменения и попробуй другую стратегию.

---

## 📝 Отчет о Проделанной Работе

После завершения выведи отчет:

| Область | Статус | Было ошибок | Исправлено | Осталось |
| :--- | :--- | :--- | :--- | :--- |
| **Frontend** | 🔴 / 🟢 | X | Y | Z |
| **Backend** | 🔴 / 🟢 | X | Y | Z |

**Детальный лог исправлений (примеры):**
* `front/src/App.tsx`: Исправлен проп `user` (добавлен `User | null`).
* `back/main.py`: Исправлен импорт `FastAPI`.

## Критерии Успеха
1.  `npm run type-check` завершается с кодом 0.
2.  `mypy` завершается с кодом 0.
3.  `ruff` не находит нарушений.
4.  Код не содержит новых `any` или `ignore`.

Запускай процесс.