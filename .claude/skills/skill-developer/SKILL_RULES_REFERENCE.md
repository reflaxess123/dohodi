# skill-rules.json - Полный справочник

Полная схема и справочник конфигурации для `.claude/skills/skill-rules.json`.

## Оглавление

- [Расположение файла](#расположение-файла)
- [Полная TypeScript схема](#полная-typescript-схема)
- [Руководство по полям](#руководство-по-полям)
- [Пример: Защитный скилл](#пример-защитный-скилл)
- [Пример: Доменный скилл](#пример-доменный-скилл)
- [Валидация](#валидация)

---

## Расположение файла

**Путь:** `.claude/skills/skill-rules.json`

Этот JSON файл определяет все скиллы и их условия активации для системы автоматической активации.

---

## Полная TypeScript схема

```typescript
interface SkillRules {
    version: string;
    skills: Record<string, SkillRule>;
}

interface SkillRule {
    type: 'guardrail' | 'domain';
    enforcement: 'block' | 'suggest' | 'warn';
    priority: 'critical' | 'high' | 'medium' | 'low';

    promptTriggers?: {
        keywords?: string[];
        intentPatterns?: string[];  // Строки regex
    };

    fileTriggers?: {
        pathPatterns: string[];     // Glob паттерны
        pathExclusions?: string[];  // Glob паттерны
        contentPatterns?: string[]; // Строки regex
        createOnly?: boolean;       // Активировать только при создании файла
    };

    blockMessage?: string;  // Для защитных скиллов, плейсхолдер {file_path}

    skipConditions?: {
        sessionSkillUsed?: boolean;      // Пропустить если использован в сессии
        fileMarkers?: string[];          // напр., ["@skip-validation"]
        envOverride?: string;            // напр., "SKIP_DB_VERIFICATION"
    };
}
```

---

## Руководство по полям

### Верхний уровень

| Поле | Тип | Обязательно | Описание |
|------|-----|-------------|----------|
| `version` | string | Да | Версия схемы (сейчас "1.0") |
| `skills` | object | Да | Карта имя скилла → SkillRule |

### Поля SkillRule

| Поле | Тип | Обязательно | Описание |
|------|-----|-------------|----------|
| `type` | string | Да | "guardrail" (принудительный) или "domain" (рекомендательный) |
| `enforcement` | string | Да | "block" (PreToolUse), "suggest" (UserPromptSubmit), или "warn" |
| `priority` | string | Да | "critical", "high", "medium", или "low" |
| `promptTriggers` | object | Опционально | Триггеры для хука UserPromptSubmit |
| `fileTriggers` | object | Опционально | Триггеры для хука PreToolUse |
| `blockMessage` | string | Опционально* | Обязательно если enforcement="block". Используйте плейсхолдер `{file_path}` |
| `skipConditions` | object | Опционально | Способы обхода и отслеживание сессии |

*Обязательно для защитных скиллов

### Поля promptTriggers

| Поле | Тип | Обязательно | Описание |
|------|-----|-------------|----------|
| `keywords` | string[] | Опционально | Точные совпадения подстроки (регистронезависимо) |
| `intentPatterns` | string[] | Опционально | Regex паттерны для обнаружения намерения |

### Поля fileTriggers

| Поле | Тип | Обязательно | Описание |
|------|-----|-------------|----------|
| `pathPatterns` | string[] | Да* | Glob паттерны для путей файлов |
| `pathExclusions` | string[] | Опционально | Glob паттерны для исключения (напр., тестовые файлы) |
| `contentPatterns` | string[] | Опционально | Regex паттерны для сопоставления содержимого файла |
| `createOnly` | boolean | Опционально | Активировать только при создании новых файлов |

*Обязательно если fileTriggers присутствует

### Поля skipConditions

| Поле | Тип | Обязательно | Описание |
|------|-----|-------------|----------|
| `sessionSkillUsed` | boolean | Опционально | Пропустить если скилл уже использован в этой сессии |
| `fileMarkers` | string[] | Опционально | Пропустить если файл содержит маркер-комментарий |
| `envOverride` | string | Опционально | Имя переменной окружения для отключения скилла |

---

## Пример: Защитный скилл

Полный пример блокирующего защитного скилла со всеми возможностями:

```json
{
  "database-verification": {
    "type": "guardrail",
    "enforcement": "block",
    "priority": "critical",

    "promptTriggers": {
      "keywords": [
        "prisma",
        "database",
        "table",
        "column",
        "schema",
        "query",
        "migration"
      ],
      "intentPatterns": [
        "(add|create|implement).*?(user|login|auth|tracking|feature)",
        "(modify|update|change).*?(table|column|schema|field)",
        "database.*?(change|update|modify|migration)"
      ]
    },

    "fileTriggers": {
      "pathPatterns": [
        "**/schema.prisma",
        "**/migrations/**/*.sql",
        "database/src/**/*.ts",
        "form/src/**/*.ts",
        "email/src/**/*.ts",
        "users/src/**/*.ts",
        "projects/src/**/*.ts",
        "utilities/src/**/*.ts"
      ],
      "pathExclusions": [
        "**/*.test.ts",
        "**/*.spec.ts"
      ],
      "contentPatterns": [
        "import.*[Pp]risma",
        "PrismaService",
        "prisma\\.",
        "\\.findMany\\(",
        "\\.findUnique\\(",
        "\\.findFirst\\(",
        "\\.create\\(",
        "\\.createMany\\(",
        "\\.update\\(",
        "\\.updateMany\\(",
        "\\.upsert\\(",
        "\\.delete\\(",
        "\\.deleteMany\\("
      ]
    },

    "blockMessage": "⚠️ ЗАБЛОКИРОВАНО - Обнаружена операция с базой данных\n\n📋 ТРЕБУЕМОЕ ДЕЙСТВИЕ:\n1. Используйте инструмент Skill: 'database-verification'\n2. Проверьте ВСЕ имена таблиц и колонок по схеме\n3. Проверьте структуру базы данных командами DESCRIBE\n4. Затем повторите это редактирование\n\nПричина: Предотвращение ошибок в именах колонок в запросах Prisma\nФайл: {file_path}\n\n💡 СОВЕТ: Добавьте комментарий '// @skip-validation' для пропуска будущих проверок",

    "skipConditions": {
      "sessionSkillUsed": true,
      "fileMarkers": [
        "@skip-validation"
      ],
      "envOverride": "SKIP_DB_VERIFICATION"
    }
  }
}
```

### Ключевые моменты для защитных скиллов

1. **type**: Должен быть "guardrail"
2. **enforcement**: Должен быть "block"
3. **priority**: Обычно "critical" или "high"
4. **blockMessage**: Обязательно, чёткие инструкции к действию
5. **skipConditions**: Отслеживание сессии предотвращает повторные напоминания
6. **fileTriggers**: Обычно имеет и path, и content паттерны
7. **contentPatterns**: Ловит фактическое использование технологии

---

## Пример: Доменный скилл

Полный пример скилла с рекомендательным предложением:

```json
{
  "project-catalog-developer": {
    "type": "domain",
    "enforcement": "suggest",
    "priority": "high",

    "promptTriggers": {
      "keywords": [
        "layout",
        "layout system",
        "grid",
        "grid layout",
        "toolbar",
        "column",
        "cell editor",
        "cell renderer",
        "submission",
        "submissions",
        "blog dashboard",
        "datagrid",
        "data grid",
        "CustomToolbar",
        "GridLayoutDialog",
        "useGridLayout",
        "auto-save",
        "column order",
        "column width",
        "filter",
        "sort"
      ],
      "intentPatterns": [
        "(how does|how do|explain|what is|describe).*?(layout|grid|toolbar|column|submission|catalog)",
        "(add|create|modify|change).*?(toolbar|column|cell|editor|renderer)",
        "blog dashboard.*?"
      ]
    },

    "fileTriggers": {
      "pathPatterns": [
        "frontend/src/features/submissions/**/*.tsx",
        "frontend/src/features/submissions/**/*.ts"
      ],
      "pathExclusions": [
        "**/*.test.tsx",
        "**/*.test.ts"
      ]
    }
  }
}
```

### Ключевые моменты для доменных скиллов

1. **type**: Должен быть "domain"
2. **enforcement**: Обычно "suggest"
3. **priority**: "high" или "medium"
4. **blockMessage**: Не нужен (не блокирует)
5. **skipConditions**: Опционально (менее критично)
6. **promptTriggers**: Обычно имеет обширные ключевые слова
7. **fileTriggers**: Может иметь только path паттерны (контент менее важен)

---

## Валидация

### Проверка синтаксиса JSON

```bash
cat .claude/skills/skill-rules.json | jq .
```

Если валидно, jq красиво отформатирует JSON. Если невалидно, покажет ошибку.

### Распространённые ошибки JSON

**Висячая запятая:**
```json
{
  "keywords": ["one", "two",]  // ❌ Висячая запятая
}
```

**Отсутствующие кавычки:**
```json
{
  type: "guardrail"  // ❌ Отсутствуют кавычки у ключа
}
```

**Одинарные кавычки (невалидный JSON):**
```json
{
  'type': 'guardrail'  // ❌ Должны быть двойные кавычки
}
```

### Чек-лист валидации

- [ ] Синтаксис JSON валиден (используйте `jq`)
- [ ] Все имена скиллов совпадают с именами файлов SKILL.md
- [ ] Защитные скиллы имеют `blockMessage`
- [ ] Сообщения блокировки используют плейсхолдер `{file_path}`
- [ ] Паттерны намерений - валидные regex (тестируйте на regex101.com)
- [ ] Паттерны путей файлов используют корректный glob синтаксис
- [ ] Паттерны контента экранируют специальные символы
- [ ] Приоритет соответствует уровню принуждения
- [ ] Нет дублирующихся имён скиллов

---

**Связанные файлы:**
- [SKILL.md](SKILL.md) - Главное руководство по скиллам
- [TRIGGER_TYPES.md](TRIGGER_TYPES.md) - Полная документация по триггерам
- [TROUBLESHOOTING.md](TROUBLESHOOTING.md) - Отладка проблем конфигурации
