{
    "name": "claude-hooks",
    "version": "1.0.0",
    "description": "TypeScript hooks for Claude Code skill auto-activation",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "tsc skill-activation-prompt.ts --target es2020 --module es2020 --moduleResolution node --lib es2020",
        "check": "tsc --noEmit",
        "test": "node skill-activation-prompt.js < test-input.json"
    },
    "dependencies": {
        "@types/node": "^20.11.0",
        "tsx": "^4.7.0",
        "typescript": "^5.3.3"
    }
}
